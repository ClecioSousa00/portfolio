import { AboutSection } from "../AboutSection"
import { Container } from "../Container"
import { Hero } from "../Hero"

export const Main =()=>{
  return(
   
      <main>
        <Hero/>
        <AboutSection/>
      </main>
   
  )
}